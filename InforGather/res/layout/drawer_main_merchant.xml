<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <!-- The main content view -->

    <include layout="@layout/activity_offers_list" />

    <!-- The navigation drawer -->

    <ScrollView
        android:id="@+id/left_drawer_cont"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/app_theme_gradient_light_gray" >

        <RelativeLayout
            android:id="@+id/left_drawer_cont_rel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="start" >

            <include layout="@layout/activity_lokal_home_merchant" />
        </RelativeLayout>
    </ScrollView>

</android.support.v4.widget.DrawerLayout>